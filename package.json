{
  "name": "gas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clasp": "clasp",
    "tsc": "tsc",
    "build": "esbuild src/Code.ts --bundle --outdir=dist --format=iife --global-name=_entry && ./node_modules/.bin/ts-node -T build.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/clasp": "^2.5.0",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.92",
    "esbuild": "^0.24.2",
    "ts-node": "^10.9.2"
  }
}
